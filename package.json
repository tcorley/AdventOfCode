{
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "deno run -q --import-map=import_map.json --allow-read --allow-net --allow-env index.ts",
    "start:watch": "deno run -q --import-map=import_map.json --allow-read --allow-net --allow-env --watch index.ts"
  }
}
